<Window x:Class="XMLAndDBAppGUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Обработка реестров ОМС (ORM)"
        Height="720" Width="980" MinWidth="800" MinHeight="600"
        WindowStartupLocation="CenterScreen"
        Background="#2B2D31" Foreground="#DBDEE1"
        FontFamily="Segoe UI">

    <Window.Resources>
        <!-- Discord Palette -->
        <SolidColorBrush x:Key="BgDark" Color="#1E1F22"/>
        <SolidColorBrush x:Key="BgMedium" Color="#2B2D31"/>
        <SolidColorBrush x:Key="BgLight" Color="#313338"/>
        <SolidColorBrush x:Key="BgInput" Color="#383A40"/>

        <SolidColorBrush x:Key="BrandBlurple" Color="#5865F2"/>
        <SolidColorBrush x:Key="BrandBlurpleHover" Color="#4752C4"/>
        <SolidColorBrush x:Key="BrandBlurpleActive" Color="#3C45A5"/>

        <SolidColorBrush x:Key="TextNormal" Color="#DBDEE1"/>
        <SolidColorBrush x:Key="TextMuted" Color="#949BA4"/>
        <SolidColorBrush x:Key="BorderColor" Color="#1F2023"/>

        <!-- Style: GroupBox -->
        <Style TargetType="GroupBox">
            <Setter Property="Background" Value="{StaticResource BgLight}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="{StaticResource TextNormal}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="GroupBox">
                        <Border Background="{TemplateBinding Background}" 
                        BorderBrush="{TemplateBinding BorderBrush}" 
                        BorderThickness="{TemplateBinding BorderThickness}" 
                        CornerRadius="8">
                            <Grid Margin="12">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>
                                <!-- Для заголовка используем TextBlock c пробросом содержимого через TemplateBinding -->
                                <TextBlock Text="{TemplateBinding Header}" 
                                   FontWeight="Bold" 
                                   FontSize="13" 
                                   Foreground="{StaticResource TextMuted}" 
                                   Margin="0,0,0,8"/>
                                <ContentPresenter Grid.Row="1"/>
                            </Grid>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- Style: TextBox -->
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="{StaticResource BgInput}"/>
            <Setter Property="Foreground" Value="{StaticResource TextNormal}"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="CaretBrush" Value="{StaticResource TextNormal}"/>
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="1" CornerRadius="4">
                            <ScrollViewer x:Name="PART_ContentHost" Focusable="false"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource BrandBlurple}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Style: Discord Button -->
        <Style x:Key="DiscordButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource BrandBlurple}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource BrandBlurpleHover}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource BrandBlurpleActive}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Background" Value="#4E5058"/>
                                <Setter Property="Foreground" Value="#80848E"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Secondary Button Style -->
        <Style x:Key="DiscordSecondaryButton" TargetType="Button" BasedOn="{StaticResource DiscordButton}">
            <Setter Property="Background" Value="#4E5058"/>
            <Setter Property="Foreground" Value="White"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#6D6F78"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- TabControl & TabItem -->
        <Style TargetType="TabControl">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>

        <Style TargetType="TabItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource TextMuted}"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="Margin" Value="0,0,4,8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="4" Padding="{TemplateBinding Padding}">
                            <ContentPresenter x:Name="contentPresenter" ContentSource="Header" Focusable="False"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource BgInput}"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource BgLight}"/>
                                <Setter Property="Foreground" Value="{StaticResource TextNormal}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- DataGrid Discord Style -->
        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="{StaticResource BgDark}"/>
            <Setter Property="RowBackground" Value="{StaticResource BgDark}"/>
            <Setter Property="AlternatingRowBackground" Value="{StaticResource BgLight}"/>
            <Setter Property="Foreground" Value="{StaticResource TextNormal}"/>
            <Setter Property="GridLinesVisibility" Value="None"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
        </Style>

        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="{StaticResource BgLight}"/>
            <Setter Property="Foreground" Value="{StaticResource TextMuted}"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
        </Style>

        <Style TargetType="DataGridCell">
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridCell">
                        <Border Padding="{TemplateBinding Padding}" Background="{TemplateBinding Background}">
                            <ContentPresenter VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="{StaticResource BrandBlurple}"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Параметры импорта -->
        <GroupBox Header="Параметры импорта" Grid.Row="0" Margin="0,0,0,10" Padding="10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="100"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="80"/>
                    <ColumnDefinition Width="120"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="35"/>
                    <RowDefinition Height="35"/>
                </Grid.RowDefinitions>

                <TextBlock Text="XML Реестр:" VerticalAlignment="Center"/>
                <TextBox x:Name="TxtXmlPath" Grid.Column="1" VerticalAlignment="Center" Margin="5,0" Padding="3"/>
                <Button x:Name="BtnBrowseXml" Content="Обзор..." Grid.Column="2" Height="26" Click="BtnBrowseXml_Click"/>

                <!-- Кнопка очистки БД -->
                <Button x:Name="BtnClearDb" Content="🗑 Очистить БД" Grid.Column="3" Grid.RowSpan="2" Height="55" 
                Margin="10,0,0,0" Background="#CD2626" Foreground="White" FontWeight="Bold" 
                Click="BtnClearDb_Click"/>

                <TextBlock Text="XSD Схема:" Grid.Row="1" VerticalAlignment="Center"/>
                <TextBox x:Name="TxtXsdPath" Grid.Row="1" Grid.Column="1" VerticalAlignment="Center" Margin="5,0" Padding="3" Text="schema.xsd"/>
                <Button x:Name="BtnBrowseXsd" Content="Обзор..." Grid.Row="1" Grid.Column="2" Height="26" Click="BtnBrowseXsd_Click"/>
            </Grid>
        </GroupBox>
        
        <!-- Кнопка запуска -->
        <Button x:Name="BtnProcess" Content="Загрузить и обработать реестр" Grid.Row="1" Height="40" 
                Style="{StaticResource DiscordButton}" FontSize="14" 
                Margin="0,0,0,12" Click="BtnProcess_Click"/>

        <!-- Вкладки -->
        <TabControl Grid.Row="2">
            <TabItem Header="Журнал импорта">
                <RichTextBox x:Name="TxtLog" IsReadOnly="True" VerticalScrollBarVisibility="Auto" 
                             FontFamily="Consolas" FontSize="12" 
                             Background="{StaticResource BgDark}" Foreground="#2ECC71" 
                             Padding="12" BorderThickness="0"/>
            </TabItem>

            <TabItem Header="Данные из БД (ZGLV / EVENT)">
                <Border Background="{StaticResource BgDark}" CornerRadius="4" Padding="4">
                    <DataGrid x:Name="DgEvents" AutoGenerateColumns="True" IsReadOnly="True"/>
                </Border>
            </TabItem>

            <TabItem Header="Реестр пациентов (PERS)">
                <Border Background="{StaticResource BgDark}" CornerRadius="4" Padding="4">
                    <DataGrid x:Name="DgPersons" AutoGenerateColumns="True" IsReadOnly="True"/>
                </Border>
            </TabItem>
        </TabControl>

        <!-- Статус бар -->
        <StatusBar Grid.Row="3" Margin="0,12,0,0" Background="Transparent" Foreground="{StaticResource TextMuted}">
            <StatusBarItem>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Статус: " Margin="0,0,4,0"/>
                    <TextBlock x:Name="TxtStatus" Text="Готов к работе" FontWeight="Bold" Foreground="#5865F2"/>
                </StackPanel>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>
